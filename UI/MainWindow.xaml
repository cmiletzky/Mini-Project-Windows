<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.UserControls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        Title="Main" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="75*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="main_contect" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Frame x:Name="Main" NavigationUIVisibility="Hidden"></Frame>
            
            
        </Grid>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40*"/>
                <RowDefinition Height="60*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="LightSalmon">
                <Menu Background="LightSalmon" HorizontalAlignment="Right" Padding="5,3" Height="40" DockPanel.Dock="Top" VerticalAlignment="Top">
                <MenuItem FontSize="17" x:Name="menu_login" Header="הרשמה / התחברות" Click="Login_Click">

                </MenuItem>
                <MenuItem FontSize="17" x:Name="menu_account" Header="חשבון" Click="Account_Click"/>
                <MenuItem FontSize="17" x:Name="menu_logout" Header="יציאה" Click="logout_menu"/>
                <MenuItem FontSize="17" Header="צור קשר">
                    <MenuItem FontSize="16" Header="Sending an email"/>
                    <MenuItem FontSize="16" Header="Sending WhatsApp"/>
                </MenuItem>

            </Menu>
            </Border>
        </Grid>
      

    </Grid>
</Window>
